# Bundled yt-dlp

The release build downloads the official [yt-dlp](https://github.com/yt-dlp/yt-dlp) binary here automatically (`npm run bundle:deps` or any `build:*` script). Do not commit the binary to git.

For local development without running the bundle script, install `yt-dlp` on your PATH instead.
